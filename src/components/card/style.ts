import { styled } from '@mui/material'

export const StyledP = styled('h1')`
  display: flex;
  align-items: center;
  font-family: 'Poppins';
  font-weight: 600;
  gap: 1rem;
  span {
    font-size: 1.5rem;
  }
  p {
    font-size: 1rem;
  }
`
